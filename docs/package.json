{
  "name": "olapcube",
  "version": "1.0.0",
  "author": "Romain Gilliotte",
  "license": "ISC",
  "description": "A naive implementation of in-memory OLAP cubes for NodeJS and the browser.",
  "main": "dist/olap.js",
  "module": "lib/index.js",
  
  "dependencies": {
  },
  "devDependencies": {
    "esdoc": "^0.5.2",
    "mocha": "^3.2.0",
    "babel-cli": "^6.23.0",
    "babel-preset-es2015": "^6.22.0",
    "babelify": "^7.3.0"
  },
  "scripts": {
    "prepublish": "npm run clean && npm run build",
    "clean": "./node_modules/.bin/rimraf dist docs",
    "build": "mkdir -p dist && ./node_modules/.bin/browserify lib/index.js -o dist/olap.js --standalone olap -t [ babelify ]",
    "test": "./node_modules/.bin/mocha --compilers js:babel-core/register",
    "gen-doc": "./node_modules/.bin/esdoc"
  }
}
