{
  "name": "olapcube",
  "version": "1.0.0",
  "author": "Romain Gilliotte",
  "license": "ISC",
  "description": "An implementation of in-memory OLAP cubes for NodeJS and the browser.",
  "main": "dist/olap.js",
  "module": "src/index.js",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "x",
    "babel-plugin-transform-do-expressions": "^6.22.0",
    "babel-preset-env": "x",
    "babelify": "^8.0.0",
    "browserify": "^15.2.0",
    "chai": "^4.1.2",
    "esdoc": "^1.0.4",
    "mocha": "^5.0.0",
    "nyc": "^11.4.1",
    "rimraf": "x"
  },
  "scripts": {
    "prepare": "npm run clean && npm run build",
    "clean": "./node_modules/.bin/rimraf dist docs",
    "build": "mkdir -p dist && ./node_modules/.bin/browserify src/index.js -o dist/olap.js --standalone olap -t [ babelify ]",
    "test": "./node_modules/.bin/mocha --require babel-core/register",
    "gen-doc": "./node_modules/.bin/esdoc"
  }
}
