{
    "name": "olap-in-memory",
    "version": "0.0.0-development",
    "author": "Romain Gilliotte",
    "license": "Unlicense",
    "description": "An implementation of in-memory OLAP cubes for NodeJS and the browser.",
    "main": "src/index.js",
    "repository": "github:romain-gilliotte/olap-in-memory",
    "dependencies": {
        "expr-eval": "^2.0.2",
        "lodash.clonedeep": "^4.5.0",
        "lodash.merge": "^4.6.2",
        "timeslot-dag": "^2.1.3"
    },
    "devDependencies": {
        "@types/chai": "^5.2.2",
        "@types/mocha": "^10.0.10",
        "@types/node": "^24.0.4",
        "chai": "^5.1.2",
        "mocha": "^11.0.1",
        "nyc": "^17.1.0",
        "semantic-release": "^24.2.1",
        "ts-node": "^10.9.2",
        "typescript": "^5.8.3"
    },
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist",
        "test": "mocha",
        "test:dist": "npm run build && mocha",
        "coverage": "nyc --reporter=text npm test",
        "coverage:dist": "npm run build && nyc --reporter=text mocha",
        "prepublishOnly": "npm run build"
    }
}
